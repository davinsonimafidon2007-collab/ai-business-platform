<?xml version="1.0" encoding="utf-8"?>
<!--
  MOB-P1-007: Network security config — build type RELEASE (src/main).

  Production policy: cleartext HTTP is NOT permitted by default. The only
  domains allowed are the production HTTPS endpoints. For physical-device
  development against a LAN backend over HTTP, use the DEBUG build
  (src/debug/res/xml/network_security_config.xml) which permits cleartext
  and user certificates.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">localhost</domain>
    </domain-config>
</network-security-config>