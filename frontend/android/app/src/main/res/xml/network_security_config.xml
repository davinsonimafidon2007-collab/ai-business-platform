<?xml version="1.0" encoding="utf-8"?>
<!--
  Production network security config.

  Only allows cleartext to localhost and 10.0.2.2 (Android emulator alias).
  All other traffic MUST use HTTPS.

  For physical devices connecting to a LAN backend via HTTP, use the DEBUG
  build (assembleDebug) which allows cleartext to any host.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">localhost</domain>
    </domain-config>
</network-security-config>
