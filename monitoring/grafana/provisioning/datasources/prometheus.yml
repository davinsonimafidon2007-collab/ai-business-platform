# monitoring/grafana/provisioning/datasources/prometheus.yml
# Fuente de datos Prometheus provisionada automáticamente (Bloque 6 / DEVOPS).

apiVersion: 1

datasources:
  - name: Prometheus
    uid: prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
